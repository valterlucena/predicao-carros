---
title: "Predicao de preços de Carros"
author: "A equipe"
date: "5 de dezembro de 2018"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r setup, warning=FALSE, message=FALSE}
library(tidyverse)
library(here)
library(caret)

theme_set(theme_minimal())
```


```{r, warning=FALSE, message=FALSE}
treino <- read.csv(here("data/treino.csv"))
validacao <- read.csv(here("data/validacao.csv"))

```



```{r}
treino %>% 
  names()
```

```{r}
treino %>% 
  head()
```

```{r}
set.seed(379)

v <- treino[sample(nrow(treino), 10), ]
```



```{r}
treino$Year <- as.factor(treino$Year)
treino$City <- as.factor(treino$City)
treino$State <- as.factor(treino$State)
treino$Model <- as.factor(treino$Model)
treino$Make <- as.factor(treino$Make)
```


```{r}
controle <- trainControl(method = "repeatedcv",
                   number = 1,
                   repeats = 10,
                   verboseIter = TRUE)
```

```{r}
preProcessamento <- c("scale", "center", "nzv")
```

```{r}
lambdaGrid <- expand.grid(lambda = 10^seq(10, -2, length=100))
```


```{r}
modelo.ridge <- train(Price ~ .,
                      data = v,
                      trControl = controle,
                      method = "ridge")
```

